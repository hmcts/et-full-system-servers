apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: systems-admin-ingress
spec:
  rules:
    - host: admin.et.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: systems-admin
                port:
                  name: web
