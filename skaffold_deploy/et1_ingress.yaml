apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: systems-et1-ingress
spec:
  rules:
    - host: et1.et.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: systems-et1
                port:
                  name: web
