apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: systems-et3-ingress
spec:
  rules:
    - host: et3.et.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: systems-et3
                port:
                  name: web
