apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atos-ingress
spec:
  rules:
    - host: atos.et.192.168.0.44.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: atos
                port:
                  name: web
